---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bootog                  # Ingress name
  labels:
    app: bootog
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "bootog.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bootog               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
